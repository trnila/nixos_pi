name: deploy

on:
  workflow_dispatch:
  push:

jobs:
  #prek:
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@v6
  #    - uses: cachix/install-nix-action@v31
  #    - run: nix develop -c prek run --all-files

  deploy:
    runs-on: ubuntu-latest
    #needs: prek
    steps:
      - uses: actions/checkout@v6
      - run: curl -v -6 trnila.eu
