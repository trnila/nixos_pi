name: deploy

on:
  workflow_dispatch:
  push:

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: curl -6 trnila.eu
      - uses: cachix/install-nix-action@v31
